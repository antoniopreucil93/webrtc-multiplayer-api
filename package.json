{
  "name": "webrtc-data-chanel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "git add . && git commit -m 'test' && git push heroku master && heroku logs -t"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/ws": "^8.2.0",
    "uuidv4": "^6.2.12",
    "ws": "^8.2.3"
  }
}
